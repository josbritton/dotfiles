(defpoll longdate :interval "10s"
    "date '+%a, %d %b %Y'")

(deflisten time "clock")

(defpoll date :interval "10s"
    "date '+%Y-%m-%d'")

(include "config.yuck")
(include "bar/river.yuck")
;;(include "bar/bspwm.yuck")

(defvar config-version
    1.0)
